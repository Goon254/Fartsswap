# Vercel (Production + Preview) and local .env.local — copy keys, paste values in dashboard locally.

# Railway API origin (required in production on Vercel)
FARTS_API_BASE_URL=https://your-service.up.railway.app

# Same as backend OPS_CONSOLE_SECRET (optional — moderation lab BFF)
OPS_CONSOLE_SECRET=

NEXT_PUBLIC_LAUNCH_MODE=false
